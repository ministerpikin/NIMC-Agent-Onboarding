package com.nimc.agentonboarding.ui;

import androidx.appcompat.app.AppCompatDelegate;

public class ThemeHelper {
    public static void applyMode(int mode) {
        AppCompatDelegate.setDefaultNightMode(mode);
    }
}
